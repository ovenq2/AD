@model ADUserManagement.Models.Domain.UserCreationRequest
@{
    ViewData["Title"] = "Kullanıcı Açma Talebi Detayları";
}

<div class="row">
    <div class="col-md-8 mx-auto">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h4 class="mb-0">
                    <i class="bi bi-person-plus"></i> Kullanıcı Açma Talebi Detayları
                </h4>
            </div>
            <div class="card-body">
                <table class="table table-bordered">
                    <tr>
                        <th width="30%">Talep Numarası:</th>
                        <td><code>@Model.RequestNumber</code></td>
                    </tr>
                    <tr>
                        <th>İsim:</th>
                        <td>@Model.FirstName</td>
                    </tr>
                    <tr>
                        <th>Soyisim:</th>
                        <td>@Model.LastName</td>
                    </tr>
                    <tr>
                        <th>Kullanıcı Adı:</th>
                        <td>@Model.Username</td>
                    </tr>
                    <tr>
                        <th>E-posta:</th>
                        <td>@Model.Email</td>
                    </tr>
                    <tr>
                        <th>Telefon:</th>
                        <td>@Model.Phone</td>
                    </tr>
                    <tr>
                        <th>Lokasyon:</th>
                        <td>@Model.Location</td>
                    </tr>
                    <tr>
                        <th>Birim:</th>
                        <td>@Model.Department</td>
                    </tr>
                    <tr>
                        <th>Unvan:</th>
                        <td>@Model.Title</td>
                    </tr>
                    <tr>
                        <th>Şirket:</th>
                        <td>@(Model.Company?.CompanyName ?? "Bilinmiyor")</td>
                    </tr>
                    <tr>
                        <th>Açıklama:</th>
                        <td>@(string.IsNullOrEmpty(Model.Description) ? "-" : Model.Description)</td>
                    </tr>
                    <tr>
                        <th>Talep Eden:</th>
                        <td>@(Model.RequestedBy?.DisplayName ?? "Bilinmiyor")</td>
                    </tr>
                    <tr>
                        <th>Talep Tarihi:</th>
                        <td>@Model.RequestedDate.ToString("dd.MM.yyyy HH:mm")</td>
                    </tr>
                    <tr>
                        <th>Durum:</th>
                        <td>
                            @if (Model.StatusId == 1)
                            {
                                <span class="badge bg-warning">Beklemede</span>
                            }
                            else if (Model.StatusId == 2)
                            {
                                <span class="badge bg-success">Onaylandı</span>
                            }
                            else if (Model.StatusId == 3)
                            {
                                <span class="badge bg-danger">Reddedildi</span>
                            }
                        </td>
                    </tr>
                    @if (Model.ApprovedDate.HasValue)
                    {
                        <tr>
                            <th>İşlem Tarihi:</th>
                            <td>@Model.ApprovedDate.Value.ToString("dd.MM.yyyy HH:mm")</td>
                        </tr>
                        <tr>
                            <th>İşlemi Yapan:</th>
                            <td>@(Model.ApprovedBy?.DisplayName ?? "Bilinmiyor")</td>
                        </tr>
                    }
                    @if (!string.IsNullOrEmpty(Model.RejectionReason))
                    {
                        <tr>
                            <th>Red Sebebi:</th>
                            <td class="text-danger">@Model.RejectionReason</td>
                        </tr>
                    }
                </table>
                
                <div class="mt-3">
                    <a href="/Request/MyRequests" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Geri Dön
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>